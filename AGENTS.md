# AGENTS.md

Instructions for coding agents working in this repository.

## Backend

Use `uv` for dependency management (run from the backend directory):

```
uv sync
uv add <PACKAGE-NAME>
uv run <COMMAND>
```

## Frontend

Use `npm`, run from `frontend/`.

## Workflow

- Commit code to git regularly.
